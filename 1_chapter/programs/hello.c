#include <stdio.h> //tells the compiler to include information about the standard input/output library.

int main() //where your program begins executing, required in every program.
{
  printf("hello, world\n"); //call to library function printf
}
